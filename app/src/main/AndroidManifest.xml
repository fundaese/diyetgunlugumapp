<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.casperpc.diyetgunlugum">
    //kulanıcının resimlerine ulasmak icin:
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    //uygulamanın internetin durumu hakkında bilgi alması için:
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    //uygulamanın interneti kullanması için:
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FeedActivity" />
        <activity android:name=".UploadActivity" />
        <activity android:name=".ExplanationActivity"></activity>
    </application>

</manifest>